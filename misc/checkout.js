function checkout(cartId) {
  // Pay and update stock
}

// Usage
checkout(cartId);

// --------------------------

function pay(cartId) {
  // Pay
}

function updateStock(cartId) {
  // Update stock
}

// Usage
pay(cartId);
updateStock(cartId);
